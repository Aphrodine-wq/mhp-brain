mhp.db
__pycache__/
*.pyc
mhp.db.bak-*
*.bak-*
.backups/

# QuickBooks OAuth tokens — NEVER commit (refresh token = months-long key to the books)
.qb_tokens.json

# QuickBooks exported data — local working copy, not for git
qb_data/
