mhp.db
__pycache__/
*.pyc
mhp.db.bak-*
*.bak-*
.backups/

# client-financials data dump — kept local, regenerated from source (not in git history)
export/

# QuickBooks tokens + key — NEVER commit (refresh token = months-long key to the books)
.qb_tokens.enc
.qb_key
.qb_tokens.json

# Local env: QB client creds + QuotaGuard proxy password — NEVER commit
.env
.vercel

# QuickBooks exported data — local working copy, not for git
qb_data/
